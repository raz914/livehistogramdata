VITE_API_BASE_URL=http://localhost:3001
ADMIN_RESET_KEY=4133

# Allow multiple submissions per browser session (for client testing only)
# ALLOW_MULTIPLE_SUBMISSIONS_PER_SESSION=true

# Seconds to wait before same IP can submit again (default: 30). Use 0 for no cooldown.
# IP_COOLDOWN_SECONDS=30
